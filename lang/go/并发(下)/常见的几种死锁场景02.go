package main
func main() {
    c:=make(chan int)
    c<-88
    go func() {
        <-c
    }()
}
// 这里会造成死锁，因为主协程在向通道写入数据后，
// 没有其他协程来读取通道中的数据，
// 导致主协程一直阻塞在写入操作上，无法继续执行。
// 解决方法是确保至少有一个协程来读取通道中的数据，
// 或者使用带缓冲的通道来避免阻塞。